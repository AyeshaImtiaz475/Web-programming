#Set is the collection of the unordered items
# Each element is the set must be unique & immutable

collection = {1,2,3,4, "hello", "world", 55, 55,4,5}
print(collection)
print(type(collection))
print(len(collection)) # total number of items 

#empty set syntax 
mycollection = {} # empty dictionary 
print(type(mycollection))

mycoll = set()
print(mycoll)

#add 
#sets -> immutable 
# set -> elements -> immutable 

